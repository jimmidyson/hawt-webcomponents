language: node_js
node_js:
    - "0.10"
before_install: npm install -g grunt-cli bower
install:
  - npm install
  - bower install
before_script: grunt build
cache:
  directories:
    - node_modules/
    - app/bower_components/
sudo: false
